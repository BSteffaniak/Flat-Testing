package "bank"

import "flatlang/database/ResultSet"

class Estimator {
    public static getMonthlyProfit(ResultSet set) -> Double {
        for (Int i = 0; i < set.numRows; i++) {
            //String date  = set.rows[0][0]
            //Int    month = Int.parseInt(date.getDataBetween("/", "/"))


        }
    }
}